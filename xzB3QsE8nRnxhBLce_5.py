
import math
​
def sine(x, y):
  return round(math.sin(math.radians(y))*x, 2)
​
def cosine(x, y):
  return round(math.cos(math.radians(y))*x, 2)
​
def tangent(x, y):
  return round(math.tan(math.radians(y))*x, 2)

