
import math
​
def sine(x, y):
  num = (math.sin(math.radians(y))* x)
  return round(num,2)
​
def cosine(x, y):
  num = (math.cos(math.radians(y))* x)
  return round(num,2)
​
def tangent(x, y):
  num = (math.tan(math.radians(y))* x)
  return round(num,2)

